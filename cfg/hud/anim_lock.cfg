// this alias instantly opens and closes the voice menu
// which triggers the MenuOpen and MenuClose animations
// which most animation locks will often be set up with

alias anim_lock "voice_menu_1; slot10"