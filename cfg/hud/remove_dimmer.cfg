// this loads the DashboardDimmer element (via the showconsole)
// then reloads the main menu so that it applies width/height 0
// this requires the corresponding part of mainmenuoverride.res

wait 5; showconsole; wait; incrementvar mat_antialias -100 100 1; hideconsole; wait 5; incrementvar mat_antialias -100 100 -1; echo "dimmer disabled"